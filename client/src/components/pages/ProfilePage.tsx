import React, { useState } from "react";
import styles from "@/styles/pages/User.module.scss";

type Props = {};

const ProfilePage = (props: Props) => {
	return <section className={`${styles.user_block}`}>ProfilePage</section>;
};

export default ProfilePage;
